{% extends 'base.html.twig' %}

{% block title %}Liste des livres{% endblock %}
{% block scripts %}



    {{ knp_pagination_rel_links(livres) }}
{% endblock %}

{% block body %}

    <h1>Liste des livres</h1>
    <div class="d-flex justify-content-end">
        <a href="{{ path('app_admin_livre_add') }}" class="btn btn-primary">Ajouter un livre</a>

    </div>



    <table class="mt-3 table table-hover">
        <thead class="table-primary">
        <tr class="">
            <th>Id</th>
            <th>Image</th>
            <th>Titre</th>
            <th>Categorie</th>

            <th>ISBN</th>

            <th>Editeur</th>
            <th>Prix</th>

            <th class="text-center">Voir détail</th>
            <th class="text-center">Supprimer</th>
            <th class="text-center">Modifier</th>
        </tr>
        </thead>
        <tbody>
        {% for livre in livres %}
            <tr class="align-middle">
                <td>
                    {{ livre.id }}
                </td>
                <td>
                    <img class="img-thumbnail" style="width: 100px" src="{{ livre.image }}" alt="image du livre">
                </td>
                <td>
                    {{ livre.titre }}
                </td><td>
                    {{ livre.categorie.libelle }}
                </td>
                <td>
                    {{ livre.isbn }}
                </td>

                <td>
                    {{ livre.editeur }}
                </td>
                <td>
                    {{ livre.prix }}
                </td>
                <td class="text-center"><a href="{{ path('app_admin_livre_show',{'id': livre.id}) }}" ><i class="fas fa-eye"></i></a></td>
                <td class="text-center"><a href="{{ path('app_admin_livre_delete',{'id': livre.id}) }}" ><i class="fas fa-trash-alt text-danger"></i></a></td>
                <td class="text-center"><a href="{{ path('app_admin_livre_update',{'id': livre.id}) }}"><i class="fas fa-pen text-primary"></i></a></td>
            </tr>

        {% endfor %}
        </tbody>
    </table>
    <div class="pagination justify-content-center">
        {{ knp_pagination_render(livres) }}
    </div>
{% endblock %}
